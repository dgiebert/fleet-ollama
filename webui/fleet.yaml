
defaultNamespace: ollama

labels:
  app: ollama-webui

# Custom helm options
helm:
  releaseName: ollama-webui
  chart: oci://ghcr.io/dgiebert/charts/ollama-webui
  version: 1.0.0
  values:
    persistence:
      enabled: true