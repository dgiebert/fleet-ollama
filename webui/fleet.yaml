
defaultNamespace: ollama

labels:
  app: ollama-webui

# Custom helm options
helm:
  releaseName: ollama-webui
  chart: oci://ghcr.io/dgiebert/ollama-webui
  version: main
  values:
    persistence:
      enabled: true