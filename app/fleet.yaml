
defaultNamespace: ollama

labels:
  app: ollama

# Custom helm options
helm:
  releaseName: ollama
  chart: ollama
  repo: https://otwld.github.io/ollama-helm/
  version: 1.47.0
  values:
    persistentVolume:
      enabled: true